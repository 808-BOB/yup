{
  "builds": [
    {
      "src": "starter-app/apps/web/package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "cd ../../../ && pnpm install && pnpm --filter web build"
      }
    }
  ]
} 